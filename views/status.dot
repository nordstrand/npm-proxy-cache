<html>
    <h1>Cached http requests</h1>
    <p>Cache entries: {{=it.cache.size}}</p>
    <p>Cache size: {{=it.cache.values().map(function(b) { return b.length;})}}</p>
    <table>
        {{ for(var url of it.cache.keys()) { }}
        {{ var data = it.cache.get(url); }}
            <tr><td>{{=url}}</td><td>{{=data.length}}</td></tr>
        {{ } }}
    </table>
</html>